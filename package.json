{
  "name": "project-meteor",
  "version": "8.8.8",
  "description": "This is a Meteor.js project",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.23.0",
    "axios": "^1.6.0",
    "meteor-node-stubs": "^1.2.0",
    "ws": "^8.14.0"
  },
  "meteor": {
    "mainModule": {
      "server": "server/main.js"
    }
  }
}
